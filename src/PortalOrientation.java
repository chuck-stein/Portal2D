/**
 * The four different positions in which a Portal can be oriented: FROM_BOTTOM = facing below its
 * Wall, FROM_TOP = facing above its Wall, FROM_LEFT = facing to the left of its wall, FROM_RIGHT
 * = facing to the right of its wall
 */
public enum PortalOrientation {
  FROM_BOTTOM, FROM_TOP, FROM_LEFT, FROM_RIGHT
}
